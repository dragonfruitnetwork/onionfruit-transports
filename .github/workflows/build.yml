name: Build
on: push

jobs:
  build:
    runs-on: windows-latest
    
    strategy:
        matrix:
          include:
            - repo: 'https://git.torproject.org/pluggable-transports/meek.git'
              directory: 'meek-client'
              artifact-target: 'meek.exe'

            - repo: 'https://git.torproject.org/pluggable-transports/obfs4.git'
              directory: './'
              artifact-target: 'obfs4.exe'   
              
    env:
      GOARCH: 386
      GOOS: windows

    steps:
      - name: Clone Target Repo
        run: git clone ${{ matrix.repo }} ${{ github.workspace }}

      - name: Build
        working-directory: ${{ github.workspace }}\${{ matrix.directory }}
        run: go build -o ${{ github.workspace }}\${{ matrix.artifact-target }}

      - name: Upload Artifact
        uses: actions/upload-artifact@v2
        with:
          name: ${{ matrix.artifact-target }}
          path: ${{ github.workspace }}\${{ matrix.artifact-target }}
